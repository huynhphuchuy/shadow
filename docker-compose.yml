version: "2"
services:
  server:
    container_name: shadow
    restart: always
    build: .
    ports:
      - "6969:6969"
  #   links:
  #     - mongo
  # mongo:
  #   container_name: mongo
  #   image: mongo
  #   volumes:
  #     - ./data:/data/db
  #   ports:
  #     - "27017:27017"