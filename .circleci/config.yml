version: 1
jobs:
  build:
    docker:
      - image: golang:latest
    branches:
      only:
        - master
    steps:
      - checkout:
          path: $GOPATH/src/shadow
      - run:
          name: Run
          command: |
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            cd $GOPATH/src/shadow
            ls
            dep ensure
            sh ./SERVER.sh